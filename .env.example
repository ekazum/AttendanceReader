# -----------------------------------------------------------------------
# AttendanceReader – Google Cloud Document AI configuration template
# -----------------------------------------------------------------------
# Copy this file to ".env" in the same directory as the application,
# then fill in your real values.  Never commit .env to source control.
# -----------------------------------------------------------------------

# Your GCP project ID (visible in the Google Cloud Console).
PROJECT_ID=your-gcp-project-id

# Document AI processor region – "us" or "eu".
LOCATION=us

# The Document AI processor ID (not the full resource name).
PROCESSOR_ID=your-processor-id

# GCS bucket used for uploading the PDF and storing batch output.
# The bucket must exist and be in the same region as the processor.
GCS_BUCKET_NAME=your-gcs-bucket-name

# Path to the GCP service-account JSON key file.
# This file must NOT be committed to source control.
GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\your-service-account-key.json
